{{--  Main page header that takes in: title  --}}
<div class='row page-title'>
    <div class='col-12 mb-3'>
        <h4 style='letter-spacing: 1px;'>
        	<strong>{{ title }}</strong>
        	@if(loading)
        		<i id='spinner' class="fas fa-circle-notch fa-spin fa-1x"></i>
        	@endif
        </h4>
        @if(subtitle)
            <p style='letter-spacing: 1px;'>{{ subtitle }}</p>
        @endif
    </div>
</div>
