<div class='row'>
	<div class='col'>
		@component('components.smallCard', room = room)
			@slot('body')
				<div class='mb-2'>
					@!component('components.stars', rating = room.averageRating)
				</div>
				<div>
					<span class='mr-3'>{{ antl.formatMessage('roomSearchCard.tableSeats') }}: {{room.seats}}</span>
					<span>{{ antl.formatMessage('roomSearchCard.capacity') }}: {{room.capacity}}</span>
				</div>
			@endslot
		@endcomponent
	</div>
</div>